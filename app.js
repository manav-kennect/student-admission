let lighter = require('../lighters/index.js');
// let lighter = require('lighters');
lighter.glow(async () => {
    log.d('Example Lighters Server running');
    // Services & Models are global objects
})
